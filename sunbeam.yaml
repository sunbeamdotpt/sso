schema: 1
project:
  name: sso
  kind: node-ui
  description: "Sunbeam SSO portal — login, logout, consent, 2FA and account recovery."
targets:
  build:
    exec: "deno task build"
  test:
    exec: "deno task test"
  lint:
    exec: "deno task lint"
  fmt:
    exec: "deno task fmt"
  dev:
    exec: "deno task dev"
  clean:
    exec: "rm -rf dist styled-system node_modules deno.lock"
  doc:
    exec: skip
  package:
    exec: "docker buildx build --push -t src.${SUNBEAM_REGISTRY}/studio/sso:latest ."
deps:
  services: [kratos]
outputs:
  - dist
